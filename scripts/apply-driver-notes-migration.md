# Aplicar migración 005 — Observaciones internas de conductor

1. Abre el **SQL Editor** del proyecto Supabase compartido.
2. Ejecuta `supabase/migrations/005_driver_internal_notes.sql`.
3. Verifica: columna `drivers.internal_notes` (text, nullable).

Requerido por PANEL-DRIVERS-003 (Ficha → Información → Observaciones internas).
